#include <stdio.h> // It is a preprocessor directive..It is done before compiling / processing of code
                   // so preprocessing is impt..Wherever it is present in code..
                   // this will be preprocessed before the code itself is compiled/preprocessed ,,
                   // because it has the definitions of functitons that are used in code..
                   // if this is not included..compiler will give error

int main() 
{
      printf("Hello World\n");
      return 0;

}