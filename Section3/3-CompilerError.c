#include <stdio.h>  // If # is omitted, expected this that...error

int main ()
{
    printf("Hello world\n" ); //if spelling is missed..semantic errors
    return 0; //if ; is missed it reflects that you have to use ;
    
}       

// So errors are of diff types -syntax or semantic